<UserControl x:Class="che_system.custom_controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="ValueTextBox" Grid.Column="0" 
                 Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                 VerticalContentAlignment="Center" 
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 Padding="5,0" 
                 TextChanged="ValueTextBox_TextChanged"/>
        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
            <Button x:Name="UpButton" Content="▲" 
                    Click="UpButton_Click" 
                    Height="15" Width="20" 
                    Margin="0,0,0,1" 
                    Padding="0"/>
            <Button x:Name="DownButton" Content="▼" 
                    Click="DownButton_Click" 
                    Height="15" Width="20" 
                    Padding="0"/>
        </StackPanel>
    </Grid>
</UserControl>
