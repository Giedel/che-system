<!-- Card.xaml -->
    
    <UserControl x:Class="che_system.custom_controls.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:che_system.custom_controls" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Border Style="{StaticResource CardStyle}">
            <Grid Margin="20 20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width=".75*"/>
                    <ColumnDefinition Width=".25*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Section -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <Grid Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Title}" Style="{StaticResource CardTitleStyle}"/>
                    </Grid>

                    <!-- Value -->
                    <Grid Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Value}" Style="{StaticResource CardValueStyle}"/>
                    </Grid>

                    <!-- Year Filter -->
                    <Grid Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,10">
                        <ComboBox ItemsSource="{Binding Years}"
                                  SelectedItem="{Binding SelectedYear, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Width="120"
                                  FontSize="14"
                                  Visibility="{Binding Years.Count, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>

                </Grid>

                <!-- Right Section -->
                <Grid Grid.Column="1">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <fa:IconBlock Icon="{Binding Icon}" Style="{StaticResource CardIconStyle}"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
