<Window x:Class="che_system.modals.view.Return_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Process Item Returns" Height="600" Width="800" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select Active Slip to Return" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <ListBox ItemsSource="{Binding ActiveSlips}" SelectedItem="{Binding SelectedSlip}" Height="150" Margin="0,0,0,10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding BorrowerName}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SubjectCode}" Foreground="Gray"/>
                            <TextBlock Text="{Binding DateOfUse, StringFormat=Date: {0:yyyy-MM-dd}}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="Slip Details - Enter Returned Quantities" FontWeight="Bold" Margin="0,0,0,5"/>

            <DataGrid ItemsSource="{Binding SlipDetails}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item Name" Binding="{Binding ItemName}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Borrowed" Binding="{Binding QuantityBorrowed}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Returned" Binding="{Binding QuantityReturned, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Complete Return" Command="{Binding CompleteReturnCommand}" Width="120" Margin="0,0,10,0" Background="LightGreen"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
