<UserControl x:Class="che_system.view.Return_Damages_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:che_system.view"
             xmlns:Stat_View="clr-namespace:che_system.custom_controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

        </Grid>
        
        <Grid Grid.Row="1">
            <ItemsControl ItemsSource="{Binding QuickStats}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Stat_View:Card_1 HorizontalAlignment="Stretch"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        
        <Grid Grid.Row="2">
            <Border Style="{StaticResource CardStyle}">
<Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Record Damage" Style="{StaticResource OriginalButtonStyle}" Command="{Binding RecordDamageCommand}" Margin="10"/>
                    </StackPanel>

    <StackPanel Grid.Row="1" Margin="10">

        <TextBlock Text="Unsettled Damages" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>
                        <DataGrid ItemsSource="{Binding UnsettledIncidents}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Item Name" Binding="{Binding ItemId}" Width=".4*"/>
                                <DataGridTextColumn Header="Group" Binding="{Binding GroupId}" Width=".4*"/>
                                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width=".4*"/>
                                <DataGridTextColumn Header="Date of Incident" Binding="{Binding DateOfIncident, StringFormat={}{0:yyyy-MM-dd}}" Width=".4*"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                                <DataGridTemplateColumn Header="Actions" Width=".4*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Settle" Style="{StaticResource OriginalButtonStyle}" Command="{Binding DataContext.SettleIncidentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
    </StackPanel>
</Grid>
    </Border>
</Grid>
</Grid>
</UserControl>
