<!-- Login_View.xaml -->
    
    <Window x:Class="che_system.view.Login_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:che_system.view"
        xmlns:view_model="clr-namespace:che_system.view_model"
        xmlns:custom_controls="clr-namespace:che_system.custom_controls"
        mc:Ignorable="d"
        Title="login_view" Height="700" Width="1000"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        MouseDown="Window_MouseDown">

    <Window.DataContext>
        <view_model:Login_View_Model />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="boolean_to_visibility"/>
    </Window.Resources>
    
    <Window.Visibility>
        <Binding Path="Is_View_Visible" Mode="TwoWay" Converter="{StaticResource boolean_to_visibility}"/>
    </Window.Visibility>

    <Border CornerRadius="12"
            Background="{StaticResource BackgroundBrush}">
        <Border 
                CornerRadius="10"
                BorderThickness="1"
                Opacity="0.92"
                BorderBrush="{StaticResource BorderGradient}"
                Background="{StaticResource BackgroundGradient}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="5"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="minimize_button"
                            BorderThickness="0"
                            Foreground="{StaticResource BgLight}"
                            FontSize="16"
                            FontFamily="Segoe UI"
                            Grid.Column="1"
                            Click="Minimize_Button_Click">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE921;" FontSize="10"/>

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BgLight}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        Margin="2"
                                        CornerRadius="5">
                                    <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>

                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>

                    <Button x:Name="close_button"
                            BorderThickness="0"
                            Foreground="{StaticResource BgLight}"
                            FontSize="16"
                            FontFamily="Segoe UI"
                            Grid.Column="2"
                            Click="Close_Button_Click">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8BB;" FontSize="10"/>

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BgLight}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        Margin="2"
                                        CornerRadius="5">
                                    <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>

                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                VerticalAlignment="Center">
                        <Image Source="/assets/University_of_Mindanao_Logo.png"
                           Width="150"
                           Height="150"/>
                        <TextBlock Text="CHEMISTRY LABORATORY"
                               Foreground="{StaticResource Color1}"
                               FontSize="50"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               FontWeight="Medium"
                               HorizontalAlignment="Center"
                               Margin="0,30,0,0"/>
                    </StackPanel>

                    <StackPanel Width="350"
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Orientation="Vertical"
                                Background="Transparent">



                        <TextBlock Text="Username"
                               Foreground="{StaticResource Color1}"
                               FontSize="30"
                               FontWeight="Medium"
                               FontFamily="Segoe UI"
                               Margin="0,35,0,0"/>

                    <TextBox x:Name="user_text"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="25"
                             FontWeight="Medium"
                             FontFamily="Segoe UI"
                             Background="Transparent"
                             Foreground="{StaticResource BgLight}"
                             CaretBrush="{StaticResource Color1}"
                             BorderBrush="{StaticResource BgLight}"
                             BorderThickness="0,0,0,1"
                             Height="Auto"
                             VerticalContentAlignment="Center"
                             Padding="0 5"
                             Margin="0,5,0,0"/>

                    <TextBlock Text="Password"
                               Foreground="{StaticResource Color1}"
                               FontSize="30"
                               FontWeight="Medium"
                               Margin="0,35,0,0"/>

                    <custom_controls:Bindable_Password_Box
                        Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Foreground="{StaticResource BgLight}"
                        Height="Auto"
                        FontSize="25"
                        Margin="0,5,0,0">
                        
                    </custom_controls:Bindable_Password_Box>

                    <TextBlock Text="{Binding Error_Message}"
                               Foreground="{StaticResource ErrorBrush}"
                               FontSize="15"
                               FontWeight="Medium"
                               FontFamily="Segoe UI"
                               Margin="0,10,0,0"/>

                    <Button x:Name="login_button"
                            Command="{Binding Login_Command}"
                            BorderThickness="0"
                            Content="LOG IN"
                            Cursor="Hand"
                            Foreground="{StaticResource FontMaroon}"
                            FontSize="20"
                            Grid.Column="2"
                            Margin="0,20,0,0">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource Color1}" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="AntiqueWhite" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        Height="50"
                                        Width="Auto"
                                        CornerRadius="10">
                                    <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>

                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                        <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,20,0,0"
                                Background="Transparent">

                            <TextBlock Text="Forgot Password?"
                               Foreground="{StaticResource Color1}"
                               FontSize="20" />

                            <TextBlock Text="Reset"
                               Foreground="{StaticResource BgLight}"
                               FontSize="20"
                               Cursor="Hand"
                                   Margin="10,0,0,0"/>
                        </StackPanel>

                    </StackPanel>
                </Grid>

            </Grid>

        </Border>
    </Border>
</Window>